# Deprecated — do not use as a design source

The Agent Zero Character Design Sheet was withdrawn as the final Player design on 2026-09-23.
The current sources are in `../SPEC.md` → "Design sources" (Player look = ASSETS,
Guard look = LEGACY).

| File | Was |
|---|---|
| `agent_zero_design_sheet.png` | The withdrawn design sheet |
| `player_design_views.png` | Its four views cut to sprite scale (was the validator's identity reference) |
| `player_idle_base.png` | Idle base sheet built from those views |
| `player_idle_brief.md` | Idle brief written against that design |

Kept for history only. Nothing in the app or the tools reads these files.
